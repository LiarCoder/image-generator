# 问题：Release 列表顺序与语义化版本不一致

## 现象

- `v2.0.0` 显示在 `v2.0.1` 上方。

## 根因

- GitHub Releases 列表按“发布时间”排序，不按版本号排序。
- 补发旧版本 Release 会让旧版本出现在上方。

## 解决思路

- “最新标识”与“列表顺序”分开看：
  - 可将 `v2.0.1` 明确设为 Latest
  - 若要视觉上排最前，通常需要重新发布 `v2.0.1`（更新时间）

## 经验

- 尽量按版本时间顺序发布 Release，减少后补引发的排序困扰。
